sub-anchor: &test-anchor
  type: SUB_COMMAND
  description: Subcommand registered with an anchor to flatten file

commands:
  uko:
    description: Main test command
    options:
      anchor: *test-anchor

# vim:ts=2:sw=2:et
